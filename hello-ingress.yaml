apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-ingress
  namespace: hello-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    # - host: hello.com
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-service
                port:
                  number: 80
